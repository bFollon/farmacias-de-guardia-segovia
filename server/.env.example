PORT=3000
HOST=0.0.0.0
# Client-facing — safe to embed in the iOS/Android apps.
API_KEY=change-me
# Admin-only — gates POST /api/refresh and POST /api/admin/reload. Never ship this in an app.
RELOAD_KEY=change-me-too
